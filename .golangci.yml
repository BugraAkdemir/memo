run:
  timeout: 5m
  go: "1.26"

linters:
  enable:
    - govet
    - staticcheck
    - gosimple
    - unused
    - ineffassign

linters-settings: {}

issues:
  exclude-use-default: true
  max-issues-per-linter: 0
  max-same-issues: 0
