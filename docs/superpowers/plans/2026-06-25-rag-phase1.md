# RAG Phase 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Memo'nun bellek sistemini hybrid search (vektör + FTS5) ve chunking ile güçlendir; geliştirici için Debug UI ekle.

**Architecture:** Mevcut `memories` tablosuna chunk metadata kolonları eklenir; yeni `memories_fts` FTS5 sanal tablosu oluşturulur ve tüm write işlemlerinde senkronize tutulur. `RetrieveContext` hem vektör hem FTS5 araması yaparak sonuçları Reciprocal Rank Fusion ile birleştirir. Flutter tarafında Debug paneli `/api/memory/debug-search` endpoint'ini kullanır.

**Tech Stack:** Go 1.22, SQLite + sqlite-vec (vec0 extension), Flutter/Dart + Riverpod

---

## Dosya Haritası

| Dosya | Değişim | Ne için |
|---|---|---|
| `internal/models/memory.go` | Modify | `MemoryResult`'a `MatchType string` alanı ekle |
| `internal/memory/chunker.go` | Create | `chunkText()` — saf fonksiyon, bağımsız |
| `internal/memory/chunker_test.go` | Create | chunker unit testleri |
| `internal/memory/store.go` | Modify | Schema migration, SaveInteraction, ftsSearch, RRF, RetrieveContext |
| `internal/memory/store_test.go` | Modify | Hybrid search testleri ekle |
| `internal/webserver/bridge.go` | Modify | `DebugMemorySearch` interface'e ekle |
| `internal/webserver/server.go` | Modify | `/api/memory/debug-search` route |
| `internal/webserver/handlers_flutter.go` | Modify | `handleMemoryDebugSearch` handler |
| `frontend/lib/models/gpu_info.dart` | Modify | `MemorySearchResult` Dart modeli ekle |
| `frontend/lib/core/api_client.dart` | Modify | `debugMemorySearch()` metodu |
| `frontend/lib/core/l10n.dart` | Modify | Debug UI l10n stringleri |
| `frontend/lib/widgets/settings/tabs/memory_tab.dart` | Modify | "Bellek Ara" debug paneli |

---

## Task 1: MemoryResult'a MatchType Ekle

**Files:**
- Modify: `internal/models/memory.go`

- [ ] **Step 1: `MatchType` alanını ekle**

```go
type MemoryResult struct {
    ID        string  `json:"id"`
    Content   string  `json:"content"`
    Similarity float32 `json:"similarity"`
    Timestamp string  `json:"timestamp"`
    UserMsg   string  `json:"user_msg,omitempty"`
    AssistMsg string  `json:"assist_msg,omitempty"`
    MatchType string  `json:"match_type,omitempty"` // "vector", "fts", "hybrid"
}
```

- [ ] **Step 2: Build yap, compile hataları yoksa devam et**

```bash
cd /home/bugra/Belgeler/memo && go build ./...
```

Expected: no output (success)

- [ ] **Step 3: Commit**

```bash
git add internal/models/memory.go
git commit -m "feat(memory): add MatchType field to MemoryResult"
```

---

## Task 2: Chunker (Saf Fonksiyon)

**Files:**
- Create: `internal/memory/chunker.go`
- Create: `internal/memory/chunker_test.go`

- [ ] **Step 1: Failing test yaz**

```go
// internal/memory/chunker_test.go
package memory

import (
    "strings"
    "testing"
)

func TestChunkText_ShortText(t *testing.T) {
    // 10 kelime — tek parça döner, max 300 kelimeyse
    text := "bu kısa bir mesajdır yani bölünmemeli hiç"
    chunks := chunkText(text, 300, 50)
    if len(chunks) != 1 {
        t.Fatalf("short text: got %d chunks, want 1", len(chunks))
    }
    if chunks[0] != text {
        t.Fatalf("short text chunk = %q, want original", chunks[0])
    }
}

func TestChunkText_LongText(t *testing.T) {
    // 600 kelimelik metin, maxWords=300, overlap=50
    words := make([]string, 600)
    for i := range words {
        words[i] = "kelime"
    }
    text := strings.Join(words, " ")
    chunks := chunkText(text, 300, 50)
    // 600 kelime / (300-50) adım = 3 chunk bekliyoruz (yaklaşık)
    if len(chunks) < 2 {
        t.Fatalf("long text: got %d chunks, want >=2", len(chunks))
    }
    // Her chunk max 300 kelime içermeli
    for i, c := range chunks {
        wc := len(strings.Fields(c))
        if wc > 310 { // küçük tolerans
            t.Fatalf("chunk[%d] has %d words, want <= 310", i, wc)
        }
    }
}

func TestChunkText_OverlapContent(t *testing.T) {
    // Overlap: son chunk'un başı önceki chunk'un sonuyla örtüşmeli
    words := make([]string, 400)
    for i := range words {
        words[i] = fmt.Sprintf("w%d", i)
    }
    text := strings.Join(words, " ")
    chunks := chunkText(text, 200, 50)
    if len(chunks) < 2 {
        t.Skip("not enough chunks to test overlap")
    }
    // chunk[1]'in ilk 50 kelimesi chunk[0]'ın son 50 kelimesini içermeli
    chunk0Words := strings.Fields(chunks[0])
    chunk1Words := strings.Fields(chunks[1])
    last50of0 := strings.Join(chunk0Words[len(chunk0Words)-50:], " ")
    first50of1 := strings.Join(chunk1Words[:50], " ")
    if last50of0 != first50of1 {
        t.Fatalf("overlap mismatch:\nlast50_of_chunk0 = %q\nfirst50_of_chunk1 = %q", last50of0, first50of1)
    }
}
```

Not: `chunker_test.go` dosyasının başına `"fmt"` import'unu ekle.

- [ ] **Step 2: Testi çalıştır, fail olduğunu doğrula**

```bash
cd /home/bugra/Belgeler/memo && go test ./internal/memory/ -run TestChunkText -v
```

Expected: `undefined: chunkText`

- [ ] **Step 3: chunker.go implementasyonunu yaz**

```go
// internal/memory/chunker.go
package memory

import "strings"

// chunkText splits text into overlapping word-based chunks.
// maxWords: maximum words per chunk; overlapWords: words shared between consecutive chunks.
// Returns the original text as a single chunk if it fits within maxWords.
func chunkText(text string, maxWords, overlapWords int) []string {
    words := strings.Fields(text)
    if len(words) <= maxWords {
        return []string{text}
    }

    step := maxWords - overlapWords
    if step <= 0 {
        step = maxWords
    }

    var chunks []string
    for start := 0; start < len(words); start += step {
        end := start + maxWords
        if end > len(words) {
            end = len(words)
        }
        chunks = append(chunks, strings.Join(words[start:end], " "))
        if end == len(words) {
            break
        }
    }
    return chunks
}
```

- [ ] **Step 4: Testleri çalıştır, pass olduğunu doğrula**

```bash
cd /home/bugra/Belgeler/memo && go test ./internal/memory/ -run TestChunkText -v
```

Expected: 3 test PASS

- [ ] **Step 5: Commit**

```bash
git add internal/memory/chunker.go internal/memory/chunker_test.go
git commit -m "feat(memory): add word-based chunker with overlap"
```

---

## Task 3: Schema Migration — Chunk Kolonları + FTS5

**Files:**
- Modify: `internal/memory/store.go` (sadece `initSchema` ve write metodları)

Bu task sadece şema değişikliğini kapsar. `SaveInteraction` mantığı Task 5'te değişecek.

- [ ] **Step 1: `initSchema()`'ya chunk kolonlarını ekle**

`initSchema()` içindeki `CREATE TABLE IF NOT EXISTS memories` sorgusunu şöyle güncelle:

```go
if _, err := s.db.ExecContext(ctx, `
    CREATE TABLE IF NOT EXISTS memories (
        id          INTEGER PRIMARY KEY AUTOINCREMENT,
        uuid        TEXT NOT NULL UNIQUE,
        role        TEXT NOT NULL DEFAULT 'user',
        content     TEXT NOT NULL,
        timestamp   TEXT NOT NULL,
        user_msg    TEXT NOT NULL DEFAULT '',
        assist_msg  TEXT NOT NULL DEFAULT '',
        type        TEXT NOT NULL DEFAULT 'conversation',
        embedding   BLOB DEFAULT NULL,
        chunk_index INTEGER NOT NULL DEFAULT 0,
        parent_uuid TEXT NOT NULL DEFAULT '',
        total_chunks INTEGER NOT NULL DEFAULT 1
    )
`); err != nil {
    return err
}
```

Ardından migration: mevcut tabloda kolonlar yoksa ALTER TABLE ile ekle.

`initSchema()`'ya, `CREATE TABLE IF NOT EXISTS memories` bloğundan hemen sonra ekle:

```go
// Migrate: add chunk columns if they don't exist yet
for _, col := range []struct{ name, def string }{
    {"chunk_index", "INTEGER NOT NULL DEFAULT 0"},
    {"parent_uuid", "TEXT NOT NULL DEFAULT ''"},
    {"total_chunks", "INTEGER NOT NULL DEFAULT 1"},
} {
    // PRAGMA table_info does not error on missing column, just returns 0 rows
    var count int
    _ = s.db.QueryRowContext(ctx,
        "SELECT COUNT(*) FROM pragma_table_info('memories') WHERE name = ?", col.name,
    ).Scan(&count)
    if count == 0 {
        if _, err := s.db.ExecContext(ctx,
            "ALTER TABLE memories ADD COLUMN "+col.name+" "+col.def,
        ); err != nil {
            return fmt.Errorf("alter memories add %s: %w", col.name, err)
        }
        log.Printf("MEMORY: migrated column memories.%s", col.name)
    }
}
```

- [ ] **Step 2: FTS5 tablosunu oluştur**

`initSchema()`'ya `_metadata` tablosundan sonra ekle:

```go
if _, err := s.db.ExecContext(ctx, `
    CREATE VIRTUAL TABLE IF NOT EXISTS memories_fts USING fts5(
        content,
        user_msg,
        assist_msg,
        tokenize='unicode61'
    )
`); err != nil {
    return fmt.Errorf("create memories_fts: %w", err)
}
```

- [ ] **Step 3: Mevcut satırları FTS5'e migrate et**

`initSchema()`'ya ekle (vec migration bloğunun yanına):

```go
var ftsMigrated string
if err := s.db.QueryRowContext(ctx,
    "SELECT value FROM _metadata WHERE key = 'fts_migration_done'",
).Scan(&ftsMigrated); err != nil || ftsMigrated != "1" {
    go func() {
        migCtx, migCancel := context.WithTimeout(context.Background(), 60*time.Second)
        defer migCancel()
        if err := s.migrateFTS(migCtx); err != nil {
            log.Printf("MEMORY: FTS migrate: %v", err)
            return
        }
        _ = s.db.Write(migCtx, func(tx *sql.Tx) error {
            _, err := tx.Exec(
                "INSERT OR REPLACE INTO _metadata(key, value) VALUES ('fts_migration_done', '1')",
            )
            return err
        })
        log.Printf("MEMORY: FTS migration complete")
    }()
}
```

`store.go`'ya `migrateFTS` metodunu ekle:

```go
func (s *Store) migrateFTS(ctx context.Context) error {
    type row struct {
        id        int64
        content   string
        userMsg   string
        assistMsg string
    }
    rows, err := s.db.QueryContext(ctx,
        `SELECT id, content, user_msg, assist_msg FROM memories
         WHERE id NOT IN (SELECT rowid FROM memories_fts)`)
    if err != nil {
        return err
    }
    var pending []row
    for rows.Next() {
        var r row
        if err := rows.Scan(&r.id, &r.content, &r.userMsg, &r.assistMsg); err != nil {
            continue
        }
        pending = append(pending, r)
    }
    rows.Close()

    if len(pending) == 0 {
        return nil
    }

    return s.db.Write(ctx, func(tx *sql.Tx) error {
        for _, r := range pending {
            if _, err := tx.Exec(
                "INSERT INTO memories_fts(rowid, content, user_msg, assist_msg) VALUES (?, ?, ?, ?)",
                r.id, r.content, r.userMsg, r.assistMsg,
            ); err != nil {
                return fmt.Errorf("fts insert row %d: %w", r.id, err)
            }
        }
        return nil
    })
}
```

- [ ] **Step 4: `ClearAll()` metodunda FTS5'i de temizle**

```go
func (s *Store) ClearAll() error {
    return s.db.Write(context.Background(), func(tx *sql.Tx) error {
        if s.useVec {
            if _, err := tx.Exec("DELETE FROM vec_memories"); err != nil {
                return err
            }
        }
        if _, err := tx.Exec("DELETE FROM memories_fts"); err != nil {
            return err
        }
        if _, err := tx.Exec("DELETE FROM memories"); err != nil {
            return err
        }
        return nil
    })
}
```

- [ ] **Step 5: `DeleteGobFile()` metodunda FTS5'i de temizle**

```go
func (s *Store) DeleteGobFile(relPath string) error {
    return s.db.Write(context.Background(), func(tx *sql.Tx) error {
        var id int64
        err := tx.QueryRow("SELECT id FROM memories WHERE uuid = ?", relPath).Scan(&id)
        if err != nil {
            return fmt.Errorf("memory not found: %s", relPath)
        }

        if s.useVec {
            if _, err := tx.Exec("DELETE FROM vec_memories WHERE rowid = ?", id); err != nil {
                return err
            }
        }
        if _, err := tx.Exec("DELETE FROM memories_fts WHERE rowid = ?", id); err != nil {
            return err
        }
        if _, err := tx.Exec("DELETE FROM memories WHERE id = ?", id); err != nil {
            return err
        }
        return nil
    })
}
```

- [ ] **Step 6: Build ve testler geçiyor mu doğrula**

```bash
cd /home/bugra/Belgeler/memo && go build ./... && go test ./internal/memory/ -v -timeout 30s
```

Expected: tüm mevcut testler PASS (chunker testleri de dahil)

- [ ] **Step 7: Commit**

```bash
git add internal/memory/store.go
git commit -m "feat(memory): add chunk columns and FTS5 table with migration"
```

---

## Task 4: SaveInteraction — Chunking Desteği

**Files:**
- Modify: `internal/memory/store.go`

Bu task, uzun mesajları chunklayarak her biri için ayrı row+embedding+FTS5 kaydı oluşturur.

- [ ] **Step 1: Failing test yaz**

`store_test.go`'ya ekle:

```go
func TestSaveInteraction_Chunking(t *testing.T) {
    ctx := context.Background()
    dir := t.TempDir()

    store, err := NewStore(StoreConfig{
        Dir:           dir,
        Dimension:     3,
        EmbeddingFunc: testEmbedding,
    })
    if err != nil {
        t.Fatalf("NewStore() error = %v", err)
    }
    defer store.Close()

    // 350 kelimelik userMsg → 2 chunk bekliyoruz (maxWords=300, overlap=50)
    words := make([]string, 350)
    for i := range words {
        words[i] = "coffee"
    }
    longMsg := strings.Join(words, " ")

    if err := store.SaveInteraction(ctx, longMsg, "reply"); err != nil {
        t.Fatalf("SaveInteraction() error = %v", err)
    }

    // 1 mesaj ama 2 chunk → 2 row
    if store.Count() != 2 {
        t.Fatalf("Count() = %d, want 2 (chunked)", store.Count())
    }
}

func TestSaveInteraction_ShortNoChunk(t *testing.T) {
    ctx := context.Background()
    dir := t.TempDir()

    store, err := NewStore(StoreConfig{
        Dir:           dir,
        Dimension:     3,
        EmbeddingFunc: testEmbedding,
    })
    if err != nil {
        t.Fatalf("NewStore() error = %v", err)
    }
    defer store.Close()

    if err := store.SaveInteraction(ctx, "coffee beans", "arabica"); err != nil {
        t.Fatalf("SaveInteraction() error = %v", err)
    }

    // Kısa mesaj → 1 row
    if store.Count() != 1 {
        t.Fatalf("Count() = %d, want 1 (no chunking)", store.Count())
    }
}
```

- [ ] **Step 2: Testi çalıştır, fail olduğunu doğrula**

```bash
cd /home/bugra/Belgeler/memo && go test ./internal/memory/ -run TestSaveInteraction_Chunking -v
```

Expected: `Count() = 1, want 2 (chunked)`

- [ ] **Step 3: `SaveInteraction` metodunu chunking destekli hale getir**

`SaveInteraction` metodunu şöyle yeniden yaz:

```go
const chunkMaxWords = 300
const chunkOverlapWords = 50

func (s *Store) SaveInteraction(ctx context.Context, userMsg, assistantMsg string) error {
    chunks := chunkText(userMsg, chunkMaxWords, chunkOverlapWords)
    parentUUID := fmt.Sprintf("mem_%d", time.Now().UnixNano())
    totalChunks := len(chunks)

    for i, chunk := range chunks {
        if err := s.saveChunk(ctx, chunk, assistantMsg, parentUUID, i, totalChunks); err != nil {
            return fmt.Errorf("memory.SaveInteraction chunk[%d]: %w", i, err)
        }
    }
    return nil
}

func (s *Store) saveChunk(ctx context.Context, userChunk, assistantMsg, parentUUID string, chunkIndex, totalChunks int) error {
    embedStart := time.Now()
    embedding, err := s.embed(ctx, userChunk)
    if err != nil {
        return fmt.Errorf("embed: %w", err)
    }
    if len(embedding) != s.dim {
        return fmt.Errorf("embedding dimension %d != expected %d", len(embedding), s.dim)
    }
    embedDur := time.Since(embedStart)

    timestamp := time.Now().UTC().Format(time.RFC3339)
    // Chunk'lar için uuid: parentUUID_i (tek chunk ise parentUUID)
    uuid := parentUUID
    if totalChunks > 1 {
        uuid = fmt.Sprintf("%s_%d", parentUUID, chunkIndex)
    }

    // Sadece son chunk'ta assist_msg sakla (orta chunk'larda gerek yok)
    storedAssist := ""
    if chunkIndex == totalChunks-1 {
        storedAssist = assistantMsg
    }
    content := fmt.Sprintf("[%s] User: %s", timestamp, userChunk)
    if storedAssist != "" {
        content += "\nAssistant: " + storedAssist
    }

    writeStart := time.Now()
    err = s.db.Write(ctx, func(tx *sql.Tx) error {
        embedBlob := floatsToBlob(embedding)

        res, err := tx.Exec(
            `INSERT INTO memories(uuid, role, content, timestamp, user_msg, assist_msg, embedding, chunk_index, parent_uuid, total_chunks)
             VALUES (?, 'user', ?, ?, ?, ?, ?, ?, ?, ?)`,
            uuid, content, timestamp, userChunk, storedAssist, embedBlob, chunkIndex, parentUUID, totalChunks,
        )
        if err != nil {
            return fmt.Errorf("insert memory: %w", err)
        }

        rowID, err := res.LastInsertId()
        if err != nil {
            return err
        }

        if s.useVec {
            vecJSON, _ := json.Marshal(embedding)
            if _, err := tx.Exec(
                "INSERT INTO vec_memories(rowid, embedding) VALUES (?, ?)",
                rowID, string(vecJSON),
            ); err != nil {
                return fmt.Errorf("insert vector: %w", err)
            }
        }

        if _, err := tx.Exec(
            "INSERT INTO memories_fts(rowid, content, user_msg, assist_msg) VALUES (?, ?, ?, ?)",
            rowID, content, userChunk, storedAssist,
        ); err != nil {
            return fmt.Errorf("insert fts: %w", err)
        }

        return nil
    })

    log.Printf("LATENCY memory.saveChunk embed_ms=%d write_ms=%d chunk=%d/%d vec=%v",
        embedDur.Milliseconds(),
        time.Since(writeStart).Milliseconds(),
        chunkIndex+1, totalChunks,
        s.useVec,
    )

    return err
}
```

- [ ] **Step 4: Testleri çalıştır**

```bash
cd /home/bugra/Belgeler/memo && go test ./internal/memory/ -v -timeout 30s
```

Expected: tüm testler PASS

- [ ] **Step 5: Commit**

```bash
git add internal/memory/store.go internal/memory/store_test.go
git commit -m "feat(memory): chunk long messages before saving to SQLite+vec+FTS5"
```

---

## Task 5: FTS5 Search + RRF Merge

**Files:**
- Modify: `internal/memory/store.go`

Bu task `ftsSearch()`, `reciprocalRankFusion()` ve güncellenmiş `RetrieveContext()`'i kapsar.

- [ ] **Step 1: Failing test yaz**

`store_test.go`'ya ekle:

```go
func TestHybridSearch_FTSBoostsExactMatch(t *testing.T) {
    ctx := context.Background()
    dir := t.TempDir()

    store, err := NewStore(StoreConfig{
        Dir:           dir,
        Dimension:     3,
        EmbeddingFunc: testEmbedding,
    })
    if err != nil {
        t.Fatalf("NewStore() error = %v", err)
    }
    defer store.Close()

    // "arabica" kelimesini içeren bellek
    if err := store.SaveInteraction(ctx, "I love arabica coffee beans", "great choice"); err != nil {
        t.Fatalf("SaveInteraction(arabica) error = %v", err)
    }
    // "mountain" kelimesini içeren bellek
    if err := store.SaveInteraction(ctx, "mountain hiking trail boots", "nice"); err != nil {
        t.Fatalf("SaveInteraction(mountain) error = %v", err)
    }

    // "arabica" araması — FTS ile kesin eşleşme
    results, err := store.RetrieveContext(ctx, "arabica", 5, 0)
    if err != nil {
        t.Fatalf("RetrieveContext() error = %v", err)
    }
    if len(results) == 0 {
        t.Fatal("expected results, got none")
    }
    // İlk sonuç "arabica" içermeli
    if !strings.Contains(results[0].Content, "arabica") {
        t.Fatalf("top result = %q, want arabica", results[0].Content)
    }
    // MatchType dolu olmalı
    if results[0].MatchType == "" {
        t.Fatal("MatchType should not be empty")
    }
}
```

- [ ] **Step 2: Testi çalıştır, fail olduğunu doğrula**

```bash
cd /home/bugra/Belgeler/memo && go test ./internal/memory/ -run TestHybridSearch -v
```

Expected: `MatchType should not be empty` (mevcut kod MatchType doldurmadığı için)

- [ ] **Step 3: `ftsSearch()` metodunu ekle**

`store.go`'ya ekle (`vecSearch` metodunun altına):

```go
func (s *Store) ftsSearch(ctx context.Context, query string, topK int) ([]MemoryResult, error) {
    // FTS5 MATCH query — özel karakterleri escape et
    ftsQuery := escapeFTSQuery(query)

    rows, err := s.db.QueryContext(ctx, `
        SELECT m.uuid, m.content, m.timestamp, m.user_msg, m.assist_msg,
               rank
        FROM memories_fts
        JOIN memories m ON m.id = memories_fts.rowid
        WHERE memories_fts MATCH ?
        ORDER BY rank
        LIMIT ?
    `, ftsQuery, topK)
    if err != nil {
        return nil, fmt.Errorf("memory.ftsSearch: %w", err)
    }
    defer rows.Close()

    var results []MemoryResult
    for rows.Next() {
        var uuid, content, timestamp, userMsg, assistMsg string
        var rank float64
        if err := rows.Scan(&uuid, &content, &timestamp, &userMsg, &assistMsg, &rank); err != nil {
            continue
        }
        results = append(results, MemoryResult{
            ID:        uuid,
            Content:   content,
            Timestamp: timestamp,
            UserMsg:   userMsg,
            AssistMsg: assistMsg,
            Similarity: 0, // RRF aşamasında hesaplanacak
            MatchType: "fts",
        })
    }
    return results, rows.Err()
}

// escapeFTSQuery FTS5 özel karakterlerini escape eder.
// Basit yaklaşım: tırnakla çevrele ve içindeki tırnakları ikiye katla.
func escapeFTSQuery(q string) string {
    q = strings.ReplaceAll(q, `"`, `""`)
    return `"` + q + `"`
}
```

- [ ] **Step 4: `reciprocalRankFusion()` fonksiyonunu ekle**

`store.go`'ya ekle (paket düzeyinde saf fonksiyon):

```go
// reciprocalRankFusion merges two ranked result lists using RRF(k=60).
// Returns up to topK deduplicated results sorted by RRF score descending.
func reciprocalRankFusion(vecResults, ftsResults []MemoryResult, topK int) []MemoryResult {
    const k = 60.0
    scores := make(map[string]float64)     // uuid → rrf score
    best := make(map[string]MemoryResult)  // uuid → best result so far

    applyRank := func(results []MemoryResult, matchType string) {
        for rank, r := range results {
            scores[r.ID] += 1.0 / (k + float64(rank+1))
            if _, seen := best[r.ID]; !seen {
                r.MatchType = matchType
                best[r.ID] = r
            } else if matchType != best[r.ID].MatchType {
                // appeared in both lists → "hybrid"
                m := best[r.ID]
                m.MatchType = "hybrid"
                best[r.ID] = m
            }
        }
    }

    applyRank(vecResults, "vector")
    applyRank(ftsResults, "fts")

    type scored struct {
        id    string
        score float64
    }
    var ranked []scored
    for id, score := range scores {
        ranked = append(ranked, scored{id, score})
    }
    sort.Slice(ranked, func(i, j int) bool {
        return ranked[i].score > ranked[j].score
    })

    if topK > len(ranked) {
        topK = len(ranked)
    }

    out := make([]MemoryResult, 0, topK)
    for _, s := range ranked[:topK] {
        r := best[s.id]
        r.Similarity = float32(s.score) // RRF skoru similarity olarak kullan
        out = append(out, r)
    }
    return out
}
```

- [ ] **Step 5: `RetrieveContext()` metodunu hybrid hale getir**

`RetrieveContext()` metodunu şöyle güncelle (sadece arama kısmı):

```go
func (s *Store) RetrieveContext(ctx context.Context, query string, topK int, minSimilarity float32) ([]MemoryResult, error) {
    start := time.Now()

    embedStart := time.Now()
    queryEmbedding, err := s.embed(ctx, query)
    if err != nil {
        return nil, fmt.Errorf("memory.RetrieveContext: embed: %w", err)
    }
    embedDur := time.Since(embedStart)

    if len(queryEmbedding) != s.dim {
        return nil, fmt.Errorf("memory.RetrieveContext: embedding dimension %d != expected %d", len(queryEmbedding), s.dim)
    }

    if topK <= 0 {
        return nil, nil
    }

    // Fetch more candidates for RRF merging
    candidateK := topK * 3

    var vecMemories []MemoryResult
    if s.useVec {
        vecMemories, err = s.vecSearch(ctx, queryEmbedding, candidateK, minSimilarity)
    } else {
        vecMemories, err = s.goSearch(ctx, queryEmbedding, candidateK, minSimilarity)
    }
    if err != nil {
        return nil, fmt.Errorf("memory.RetrieveContext: vector search: %w", err)
    }
    // Set MatchType on vector results
    for i := range vecMemories {
        vecMemories[i].MatchType = "vector"
    }

    ftsMemories, ftsErr := s.ftsSearch(ctx, query, candidateK)
    if ftsErr != nil {
        // FTS hataları retrieve'i engellemez — sadece logla
        log.Printf("MEMORY: ftsSearch: %v", ftsErr)
        ftsMemories = nil
    }

    var memories []MemoryResult
    if len(ftsMemories) == 0 {
        memories = vecMemories
    } else {
        memories = reciprocalRankFusion(vecMemories, ftsMemories, topK)
    }

    log.Printf("LATENCY memory.retrieve total_ms=%d embed_ms=%d top_k=%d returned=%d vec=%v fts=%d",
        time.Since(start).Milliseconds(),
        embedDur.Milliseconds(),
        topK,
        len(memories),
        s.useVec,
        len(ftsMemories),
    )

    return memories, nil
}
```

- [ ] **Step 6: Testleri çalıştır**

```bash
cd /home/bugra/Belgeler/memo && go test ./internal/memory/ -v -timeout 30s
```

Expected: tüm testler PASS

- [ ] **Step 7: Commit**

```bash
git add internal/memory/store.go internal/memory/store_test.go
git commit -m "feat(memory): hybrid search with FTS5 + RRF merge in RetrieveContext"
```

---

## Task 6: Debug Search HTTP Endpoint

**Files:**
- Modify: `internal/webserver/bridge.go`
- Modify: `internal/webserver/server.go`
- Modify: `internal/webserver/handlers_flutter.go`

`DebugMemorySearch` metodu zaten `internal/app/memory.go:144`'te mevcut. Sadece bridge interface + HTTP katmanı gerekiyor.

- [ ] **Step 1: Bridge interface'e `DebugMemorySearch` ekle**

`internal/webserver/bridge.go`'daki Memory bölümüne ekle:

```go
// Memory
ClearAllMemory() error
ListMemoryFiles() []memory.GobFileInfo
DeleteMemoryFile(relPath string) error
GetMemorySettings() config.MemoryConfig
UpdateMemorySettings(topK int, minSimilarity float32) error
GetWebSearchEnabled() bool
UpdateWebSearchConfig(enabled bool) error
GetMemoryEnabled() bool
SetMemoryEnabled(enabled bool) error
DebugMemorySearch(query string) []memory.MemoryResult  // yeni satır
```

- [ ] **Step 2: Route ekle**

`internal/webserver/server.go`'daki `/api/memory/*` route bloğuna ekle:

```go
mux.HandleFunc("/api/memory/files", s.handleMemoryFiles)
mux.HandleFunc("/api/memory/clear", s.handleMemoryClear)
mux.HandleFunc("/api/memory/settings", s.handleMemorySettings)
mux.HandleFunc("/api/memory/enabled", s.handleMemoryEnabled)
mux.HandleFunc("/api/memory/debug-search", s.handleMemoryDebugSearch)  // yeni satır
```

- [ ] **Step 3: Handler ekle**

`internal/webserver/handlers_flutter.go`'daki Memory bölümüne ekle (handleMemorySettings'in hemen altına):

```go
func (s *Server) handleMemoryDebugSearch(w http.ResponseWriter, r *http.Request) {
    if r.Method != http.MethodGet || s.fullBridge == nil {
        http.Error(w, "GET only", http.StatusMethodNotAllowed)
        return
    }
    query := r.URL.Query().Get("q")
    if query == "" {
        http.Error(w, "q parameter required", http.StatusBadRequest)
        return
    }
    results := s.fullBridge.DebugMemorySearch(query)
    if results == nil {
        writeJSON(w, []struct{}{})
        return
    }
    writeJSON(w, results)
}
```

- [ ] **Step 4: Build yap**

```bash
cd /home/bugra/Belgeler/memo && go build ./...
```

Expected: no output

- [ ] **Step 5: Basit curl testi (backend çalışıyorsa)**

```bash
curl -s "http://localhost:8090/api/memory/debug-search?q=test" | head -c 200
```

Expected: `[]` veya JSON array (backend çalışmıyorsa skip et)

- [ ] **Step 6: Commit**

```bash
git add internal/webserver/bridge.go internal/webserver/server.go internal/webserver/handlers_flutter.go
git commit -m "feat(memory): expose /api/memory/debug-search endpoint"
```

---

## Task 7: Flutter API Client + Model

**Files:**
- Modify: `frontend/lib/models/gpu_info.dart`
- Modify: `frontend/lib/core/api_client.dart`

- [ ] **Step 1: `MemorySearchResult` Dart modeli ekle**

`frontend/lib/models/gpu_info.dart`'ın sonuna ekle (veya `MemoryFileInfo` altına):

```dart
class MemorySearchResult {
  final String id;
  final String content;
  final double similarity;
  final String timestamp;
  final String userMsg;
  final String assistMsg;
  final String matchType;

  const MemorySearchResult({
    required this.id,
    required this.content,
    required this.similarity,
    required this.timestamp,
    required this.userMsg,
    required this.assistMsg,
    required this.matchType,
  });

  factory MemorySearchResult.fromJson(Map<String, dynamic> json) =>
      MemorySearchResult(
        id: json['id'] as String? ?? '',
        content: json['content'] as String? ?? '',
        similarity: (json['similarity'] as num?)?.toDouble() ?? 0.0,
        timestamp: json['timestamp'] as String? ?? '',
        userMsg: json['user_msg'] as String? ?? '',
        assistMsg: json['assist_msg'] as String? ?? '',
        matchType: json['match_type'] as String? ?? '',
      );
}
```

- [ ] **Step 2: `debugMemorySearch` metodunu api_client.dart'a ekle**

`api_client.dart`'taki Memory bölümüne `setMemoryEnabled` metodunun altına ekle:

```dart
Future<List<MemorySearchResult>> debugMemorySearch(String query) async {
  final res = await _dio.get(
    '/api/memory/debug-search',
    queryParameters: {'q': query},
  );
  if (res.data is List) {
    return (res.data as List)
        .map((e) => MemorySearchResult.fromJson(e as Map<String, dynamic>))
        .toList();
  }
  return [];
}
```

- [ ] **Step 3: Flutter analizi çalıştır**

```bash
cd /home/bugra/Belgeler/memo/frontend && flutter analyze --no-fatal-infos 2>&1 | grep -E "error:|warning:" | head -20
```

Expected: ilgili yeni kodda hata yok

- [ ] **Step 4: Commit**

```bash
git add frontend/lib/models/gpu_info.dart frontend/lib/core/api_client.dart
git commit -m "feat(memory): add MemorySearchResult model and debugMemorySearch API client"
```

---

## Task 8: Flutter Debug UI

**Files:**
- Modify: `frontend/lib/core/l10n.dart`
- Modify: `frontend/lib/widgets/settings/tabs/memory_tab.dart`

- [ ] **Step 1: L10n string'lerini ekle**

`l10n.dart`'taki Türkçe bloğuna (satır ~209 civarı, `memory_min_similarity` altına) ekle:

```dart
'memory_debug_search': 'Bellek Ara (Debug)',
'memory_debug_hint': 'Bir sorgu yaz, hangi belleklerin geldiğini ve neden geldiğini gör.',
'memory_debug_placeholder': 'Sorgu...',
'memory_debug_search_btn': 'Ara',
'memory_debug_no_results': 'Sonuç bulunamadı.',
'memory_debug_match_vector': 'Vektör',
'memory_debug_match_fts': 'Kelime Eşleşmesi',
'memory_debug_match_hybrid': 'Karma (Vektör+Kelime)',
'memory_debug_score': 'Skor',
```

Aynısını İngilizce bloğuna (satır ~920 civarı) ekle:

```dart
'memory_debug_search': 'Memory Search (Debug)',
'memory_debug_hint': 'Type a query to see which memories are retrieved and why.',
'memory_debug_placeholder': 'Query...',
'memory_debug_search_btn': 'Search',
'memory_debug_no_results': 'No results found.',
'memory_debug_match_vector': 'Vector',
'memory_debug_match_fts': 'Keyword Match',
'memory_debug_match_hybrid': 'Hybrid (Vector+Keyword)',
'memory_debug_score': 'Score',
```

- [ ] **Step 2: `MemoryTab` widget'ına debug paneli ekle**

`memory_tab.dart`'ta `MemoryTabState` sınıfına aşağıdaki state'leri ekle (mevcut `_settingsInitialized` altına):

```dart
final _debugQueryController = TextEditingController();
bool _debugSearching = false;
List<dynamic> _debugResults = []; // MemorySearchResult list
String? _debugError;
```

`dispose()` metodunu güncelle:

```dart
@override
void dispose() {
  _topKController.dispose();
  _minSimilarityController.dispose();
  _debugQueryController.dispose();
  super.dispose();
}
```

`build()` metodundaki `ListView`'ın child'larına, retrieval settings kartından sonra şu bloğu ekle:

```dart
SizedBox(height: 28),
Text(
  L10n.t('memory_debug_search'),
  style: Theme.of(context).textTheme.titleMedium?.copyWith(
    fontWeight: FontWeight.bold,
    color: MemoTheme.of(context).textMain,
  ),
),
SizedBox(height: 8),
Text(
  L10n.t('memory_debug_hint'),
  style: TextStyle(color: MemoTheme.of(context).textDim, fontSize: 13),
),
SizedBox(height: 12),
Row(
  children: [
    Expanded(
      child: SizedBox(
        height: 36,
        child: TextField(
          controller: _debugQueryController,
          style: TextStyle(fontSize: 13),
          decoration: InputDecoration(
            hintText: L10n.t('memory_debug_placeholder'),
            contentPadding: EdgeInsets.symmetric(horizontal: 12),
            filled: true,
            fillColor: MemoTheme.of(context).bgApp,
            border: OutlineInputBorder(
              borderRadius: BorderRadius.circular(MemoTheme.radiusSm),
              borderSide: BorderSide(color: MemoTheme.of(context).borderSoft),
            ),
            enabledBorder: OutlineInputBorder(
              borderRadius: BorderRadius.circular(MemoTheme.radiusSm),
              borderSide: BorderSide(color: MemoTheme.of(context).borderSoft),
            ),
            focusedBorder: OutlineInputBorder(
              borderRadius: BorderRadius.circular(MemoTheme.radiusSm),
              borderSide: BorderSide(color: MemoTheme.accent),
            ),
          ),
        ),
      ),
    ),
    SizedBox(width: 8),
    SizedBox(
      height: 36,
      child: ElevatedButton(
        onPressed: _debugSearching ? null : _runDebugSearch,
        child: _debugSearching
            ? SizedBox(width: 14, height: 14, child: CircularProgressIndicator(strokeWidth: 2))
            : Text(L10n.t('memory_debug_search_btn')),
      ),
    ),
  ],
),
if (_debugError != null)
  Padding(
    padding: EdgeInsets.only(top: 8),
    child: Text(_debugError!, style: TextStyle(color: MemoTheme.red, fontSize: 12)),
  ),
if (_debugResults.isNotEmpty)
  Padding(
    padding: EdgeInsets.only(top: 12),
    child: Column(
      children: _debugResults.asMap().entries.map((entry) {
        final i = entry.key;
        final r = entry.value;
        final matchLabel = r.matchType == 'vector'
            ? L10n.t('memory_debug_match_vector')
            : r.matchType == 'fts'
                ? L10n.t('memory_debug_match_fts')
                : L10n.t('memory_debug_match_hybrid');
        return Container(
          margin: EdgeInsets.only(bottom: 8),
          padding: EdgeInsets.all(12),
          decoration: BoxDecoration(
            color: MemoTheme.of(context).bgApp,
            borderRadius: BorderRadius.circular(MemoTheme.radiusSm),
            border: Border.all(color: MemoTheme.of(context).borderSoft),
          ),
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.start,
            children: [
              Row(
                children: [
                  Text(
                    '#${i + 1}',
                    style: TextStyle(
                      fontWeight: FontWeight.bold,
                      color: MemoTheme.of(context).textDim,
                      fontSize: 12,
                    ),
                  ),
                  SizedBox(width: 8),
                  Container(
                    padding: EdgeInsets.symmetric(horizontal: 6, vertical: 2),
                    decoration: BoxDecoration(
                      color: MemoTheme.accent.withOpacity(0.15),
                      borderRadius: BorderRadius.circular(4),
                    ),
                    child: Text(
                      matchLabel,
                      style: TextStyle(
                        color: MemoTheme.accent,
                        fontSize: 11,
                        fontWeight: FontWeight.w600,
                      ),
                    ),
                  ),
                  Spacer(),
                  Text(
                    '${L10n.t('memory_debug_score')}: ${r.similarity.toStringAsFixed(3)}',
                    style: TextStyle(
                      color: MemoTheme.of(context).textDim,
                      fontSize: 11,
                    ),
                  ),
                ],
              ),
              SizedBox(height: 6),
              Text(
                r.content.length > 200 ? r.content.substring(0, 200) + '…' : r.content,
                style: TextStyle(fontSize: 12, color: MemoTheme.of(context).textMain),
              ),
              SizedBox(height: 4),
              Text(
                r.timestamp,
                style: TextStyle(fontSize: 11, color: MemoTheme.of(context).textDim),
              ),
            ],
          ),
        );
      }).toList(),
    ),
  )
else if (!_debugSearching && _debugQueryController.text.isNotEmpty && _debugError == null)
  Padding(
    padding: EdgeInsets.only(top: 12),
    child: Text(
      L10n.t('memory_debug_no_results'),
      style: TextStyle(color: MemoTheme.of(context).textDim, fontSize: 13),
    ),
  ),
```

- [ ] **Step 3: `_runDebugSearch` metodunu ekle**

`MemoryTabState` sınıfına ekle:

```dart
Future<void> _runDebugSearch() async {
  final query = _debugQueryController.text.trim();
  if (query.isEmpty) return;
  setState(() {
    _debugSearching = true;
    _debugError = null;
    _debugResults = [];
  });
  try {
    final api = ref.read(apiClientProvider);
    final results = await api.debugMemorySearch(query);
    if (mounted) {
      setState(() => _debugResults = results);
    }
  } catch (e) {
    if (mounted) {
      setState(() => _debugError = e.toString());
    }
  } finally {
    if (mounted) {
      setState(() => _debugSearching = false);
    }
  }
}
```

`api_client.dart`'ta `apiClientProvider` tanımlıysa onu kullan; yoksa `ref.read(settingsProvider.notifier)` üzerinden api client'a eriş. Mevcut provider pattern'ine bak:

```bash
grep -n "apiClientProvider\|ApiClient\|apiClient" /home/bugra/Belgeler/memo/frontend/lib/providers/settings_provider.dart | head -10
```

Bu sonuca göre doğru provider adını kullan.

- [ ] **Step 4: Flutter analizi çalıştır**

```bash
cd /home/bugra/Belgeler/memo/frontend && flutter analyze --no-fatal-infos 2>&1 | grep -E "error:" | head -20
```

Expected: hata yok

- [ ] **Step 5: Commit**

```bash
git add frontend/lib/core/l10n.dart frontend/lib/widgets/settings/tabs/memory_tab.dart
git commit -m "feat(memory): add debug search panel to Memory settings tab"
```

---

## Self-Review

### Spec Coverage

| Gereksinim | Task |
|---|---|
| 1.1 Chunking ~200-400 kelime | Task 2 + Task 4 |
| 1.1 ~50 kelime overlap | Task 2 (`chunkOverlapWords=50`) |
| 1.1 Her parça için ayrı embedding | Task 4 (`saveChunk` her chunk için embed çağırır) |
| 1.1 Metadata (hangi sohbet/mesaj) | Task 3 (`parent_uuid`, `chunk_index`, `total_chunks`) |
| 1.2 FTS5 sanal tablosu | Task 3 |
| 1.2 content, user_msg, assist_msg FTS5'e | Task 3 |
| 1.2 RetrieveContext'te hem vec hem FTS5 | Task 5 |
| 1.2 RRF ile birleştirme | Task 5 |
| 1.3 Ayarlar > Bellek > "Bellek Ara" | Task 8 |
| 1.3 Sorgu sonuçları, skorlar, kaynaklar | Task 8 |
| 1.3 "Neden bu geldi?" (skor + match türü) | Task 1 + Task 8 (`MatchType`) |

### Placeholder Taraması

Yok — tüm adımlar tam kod içeriyor.

### Tip Tutarlılığı

- `MemoryResult.MatchType string` → Task 1'de tanımlandı, Task 5'te `vecSearch`/`ftsSearch`/`reciprocalRankFusion`'da kullanıldı. ✓
- `MemorySearchResult.matchType String` → Task 7'de tanımlandı, Task 8'de `r.matchType` ile kullanıldı. ✓
- `chunkText(text string, maxWords, overlapWords int) []string` → Task 2'de tanımlandı, Task 4'te `chunkText(userMsg, chunkMaxWords, chunkOverlapWords)` ile çağrıldı. ✓
- `saveChunk()` signature → Task 4'te tanımlandı ve `SaveInteraction` içinde aynı signature ile çağrıldı. ✓
- `reciprocalRankFusion(vecResults, ftsResults []MemoryResult, topK int) []MemoryResult` → Task 5'te tanımlandı ve `RetrieveContext` içinde aynı şekilde çağrıldı. ✓

---
